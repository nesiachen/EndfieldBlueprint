import type { BeltTemplate } from "../types";

export const BELT_SPEC: BeltTemplate = { id: "belt", name: "传送带", rate: 60, type: "belt" };
export const PIPE_SPEC: BeltTemplate = { id: "pipe", name: "管道", rate: 60, type: "pipe" };
